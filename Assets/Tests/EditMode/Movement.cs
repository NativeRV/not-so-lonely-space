using System.Collections;
using System.Collections.Generic;
using NUnit.Framework;
using UnityEngine;
using UnityEngine.TestTools;

public class MovementTest
{
  // A Test behaves as an ordinary method
  [Test]
  public void StrafeRight()
  {
    //   var Movement = new Movement(new Movement.Params
    //   {
    //     moveSpeed = 2,
    //   });

    //   var actual = Movement.CalculateHorizontal(
    //     new Vector3(2, 0, 2),
    //     new Vector3(2, 0, -2),
    //     new Vector2(1, 0),
    //     1
    //   );

    //   var expected = new Vector3(1.4f, 0, -1.4f);

    //   Assert.AreEqual(expected.y, actual.x, 0.01d);
    //   Assert.AreEqual(expected.y, actual.y, 0.01d);
    //   Assert.AreEqual(expected.z, actual.z, 0.01d);
  }
}
